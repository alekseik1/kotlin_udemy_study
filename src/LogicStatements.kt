fun main(args:Array<String>) {
    // AND &&
    // OR ||
    // NOT !

    var a = 50
    var b = 20

    if(a >= b && a >= 40) {
        print("a is greater than b and a is greater than 40")
    }
}